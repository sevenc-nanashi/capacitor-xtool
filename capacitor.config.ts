import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.sevenc7c.capacitorxtool",
  appName: "capacitor-xtool",
  webDir: "dist",
  server: {
    // url: "http://192.168.10.105:5173",
  },
};

export default config;
